[project]
name = "neosofia-schemas"
version = "0.0.0"
description = "Shared JSON schemas for the Neosofia platform. This package is not published; the pyproject exists only to pin tooling for CI and local validation."
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.21",
]

[tool.uv]
package = false
